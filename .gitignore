# ---------------------------------------------------------------------------
# Large / downloaded assets (see README.md for how to obtain them)
# ---------------------------------------------------------------------------

# OpenAI GPT-2 weights downloaded by utils/gpt_download.py (~1.8 GB for 124M + 355M)
gpt2/

# Datasets that the notebooks download themselves
data/the-verdict.txt
data/sms_spam_collection/
data/sms_spam_collection.zip

# Trained checkpoints (too large to be versioned)
*.pth
data/instruction_response.pth

# generated by ch06 when it evaluates the model on the test set
data/instruction-data-with-response.json

# ---------------------------------------------------------------------------
# Generated files
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.chainlit/
.files/
